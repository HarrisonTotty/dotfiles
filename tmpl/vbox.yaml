# Virtual Box Configuration File
# ------------------------------

include:
  - 'common.yaml'

installer:
  drive: '/dev/sda'
  disable_uefi: true
  filesystems:
    - name: 'EFI'
  partitions:
    - name: 'EFI'
      size: '1G'
      typecode: 'ef00' # EFI System
    - name: 'system'
