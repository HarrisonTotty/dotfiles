#!/bin/bash
# ~/.config/scripts/theme-random
# Script to change wallpaper and colorscheme on the fly!

wal -i "$HOME/.config/wal/wallpapers" --iterative -o "$HOME/.config/scripts/wal-set" >/dev/null 2>&1
notify-send -u low "i3" "Switched wallpaper & themes." &
